### USAGE

First run precomputecast.py. This will generate a file that contains all precomputed raycast.

You can change the input parameters in params.py (including log file to execute).

Then run main.py to see the output
